#include "main.h"

/**
 * main - Check for lowercase letters
 *
 * Return: 1 for lowercase c and 0 for anythinf else
 */

int main(void)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}	
