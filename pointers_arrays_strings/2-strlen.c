#include "main.h"

/**
 * _strlen - Return the length of a string
 * @s: pointer of the string
 * Return: O
 */

int _strlen(char *s)
{
	int count = 0;
	while (*s != '\0')
	{
		count++;
		s++;
	}
	return (0);
}
