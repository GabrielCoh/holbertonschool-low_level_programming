#include "variadic_functions.h"

/**
 * sum_them - sum of all the function's parameters
 * @n: unsigned integer
 * Return: sum
 */

int sum_them_all(const unsigned int n, ...)
{
	if (n == 0)
		return (0);
	else
		return (sum);
}
